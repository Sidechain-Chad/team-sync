development:
  adapter: async

test:
  adapter: test

# Use the postgresql adapter so Action Cable can broadcast without a separate
# Redis instance. This uses Postgres LISTEN/NOTIFY and works on a single web
# dyno — fine for a small team. If you ever need to scale to multiple web
# instances, swap this back to redis and provision Render Key Value.
production:
  adapter: postgresql
