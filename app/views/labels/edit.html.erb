<%# Render the inline form into the row. card_id comes from the link query string. %>
<% card = Card.find(params[:card_id]) %>
<%= render "cards/label_form", board: @board, label: @label, card: card %>
