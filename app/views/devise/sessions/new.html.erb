<% footer = capture do %>
  <%= link_to "Create an account", new_registration_path(resource_name), class: "hover:text-white transition-colors underline-offset-2 hover:underline" %>
  <span class="text-brand-400">·</span>
  <%= link_to "Forgot password?", new_password_path(resource_name), class: "hover:text-white transition-colors underline-offset-2 hover:underline" %>
<% end %>

<%= render "devise/shared/auth_card",
           title: "Welcome back",
           subtitle: "Sign in to your TeamSync account",
           footer_links: footer do %>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-4" }) do |f| %>

    <div>
      <%= f.label :email, class: "block text-xs font-bold text-ink-700 uppercase tracking-wide mb-1.5" %>
      <%= f.email_field :email,
            autofocus: true,
            autocomplete: "email",
            placeholder: "you@example.com",
            class: "w-full px-3 py-2.5 bg-surface-50 focus:bg-surface-0 border border-line rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-brand-700 focus:border-brand-700 transition" %>
    </div>

    <div>
      <%= f.label :password, class: "block text-xs font-bold text-ink-700 uppercase tracking-wide mb-1.5" %>
      <%= f.password_field :password,
            autocomplete: "current-password",
            placeholder: "••••••••",
            class: "w-full px-3 py-2.5 bg-surface-50 focus:bg-surface-0 border border-line rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-brand-700 focus:border-brand-700 transition" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <label class="flex items-center gap-2 text-sm text-ink-500 cursor-pointer select-none">
        <%= f.check_box :remember_me, class: "rounded border-line accent-brand-600 focus:ring-brand-700" %>
        Remember me
      </label>
    <% end %>

    <%= f.submit "Sign in", class: "w-full bg-brand-600 hover:bg-brand-700 text-white font-semibold py-2.5 rounded-lg text-sm cursor-pointer shadow-sm transition-colors" %>
  <% end %>
<% end %>
